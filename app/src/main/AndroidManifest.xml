<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_cd">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".SplashScreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    <activity android:name=".Tienda" ></activity>
     <activity android:name=".catCaramelos"></activity>
     <activity android:name=".CatChocolates" ></activity>
    <activity android:name=".CategoriasP" ></activity>
    <activity android:name=".Carrito"></activity>
    <activity android:name=".Registrate" ></activity>
    <activity android:name=".Login" ></activity>
    <activity android:name=".DetalleProducto" ></activity>
    <activity android:name=".MainActivity"></activity>
    </application>
//Revision
</manifest>